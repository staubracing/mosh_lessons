// Types of Functions

// performs a task or calculates a value


//performing atask

function greet(name, lastName){ 
console.log('Hello ' + name + ' ' + lastName);  //Concatenations
}

//calculating a value

function square(number){
    return number + number
}

console.log(square(2)) // just take the square expression and put it in the argument 

//let number = square(2);  one way of doing it
//console.log(number);



