// For of lo


// For in loop:
const person = {
    name: 'mosh',
    age: 30
};

//Use the for in to irriate over the items in a object
for(let key in person)
    console.log(key, person[key]); 


//For of is used to irriate over the items of array
const colors = ['red', 'green', 'blue'];

for (let color of colors)
console.log (color);


